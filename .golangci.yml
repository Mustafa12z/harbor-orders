# CI lint for Harbor Orders services.
# Handlers predate this gate; style linters would force a wholesale rewrite
# of business logic. Keep govet/staticcheck signal only.
run:
  timeout: 5m

linters:
  disable:
    - errcheck
    - gosimple
    - ineffassign
    - unused
